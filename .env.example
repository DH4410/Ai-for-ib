# Self-hosted OpenAI-compatible model server (for example vLLM).
MODEL_BASE_URL=http://localhost:8000/v1
MODEL_NAME=ib-tutor-model
MODEL_API_KEY=

# Makes the UI usable before a GPU/model server is connected.
USE_MOCK_MODEL=true
